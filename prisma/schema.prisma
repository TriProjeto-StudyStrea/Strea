generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Cadastro {
  id String @id @default(uuid())
  name String 
  lastname  String
  email String
  number Int
  password String

}